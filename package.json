{
  "name": "taskly",
  "version": "0.0.0",
  "description": "An example integration of a ng-app with a non-ng app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-connect": "^0.2.0"
  }
}
